package com.ninezero.presentation.theme

import androidx.compose.ui.graphics.Color

// Default
val snsDefault = Color(0xFF63b790)
val snsDarkDefault = Color(0xFF4a8a6c)

val snsDefaultMedium = Color(0xFFd4ece4)
val snsDefaultLight = Color(0xFFeff8f2)

// Dark Theme Background Colors
val snsDarkBg1 = Color(0xFF1A1A1A)  // Dark Background
val snsDarkBg2 = Color(0xFF121212)  // Dark Surface
val snsDarkBg3 = Color(0xFF1E3A2F)  // Dark Component Background
val snsDarkBg4 = Color(0xFF2A2A2A)  // Dark Dialog Background

// Light Theme Background Colors
val snsLightBg1 = Color(0xFFF5F5F5)  // Light Background
val snsLightBg2 = Color(0xFFE8E8E8)    // Light Dialog Background

// Text Colors
val snsDarkText1 = Color(0xFFE1E1E1)  // Dark Text Primary
val snsDarkText2 = Color(0xFFB3B3B3)  // Dark Text Secondary
val snsLightText1 = Color(0xFF1C1B1F)  // Light Text Primary
val snsLightText2 = Color(0xFF666666)  // Light Text Secondary

// Accent Colors
val snsDarkAccent = Color(0xFF7FCCA7)

val snsSaveDefault = Color(0xFF4A90E2)  // Primary blue
val snsSaveDark = Color(0xFF357ABD)     // Darker blue for dark theme

val snsSaveMedium = Color(0xFFD4E4F7)   // Medium blue tint
val snsSaveLight = Color(0xFFEFF5FC)     // Light blue tint

// Theme Colors
val snsBackgroundLight = snsLightBg1
val snsBackgroundDark = snsDarkBg1
val snsDarkSurface = snsDarkBg2

val snsDarkOnSurface = snsDarkText1
val snsLightOnSurface = snsLightText1

// Button Colors
val snsDialogButtonText = snsLightText2
val snsDarkDialogButtonText = snsDarkText2
val snsDialogButtonBackground = snsLightBg2
val snsDarkDialogButtonBackground = snsDarkBg4

// SmallButton Colors
val snsSmallButtonLightBackground = snsDefaultLight
val snsSmallButtonDarkBackground = snsDarkBg3
val snsSmallButtonLightText = snsDefault
val snsSmallButtonDarkText = snsDarkAccent

// SaveButton Colors
val snsSaveButtonLightBackground = snsSaveLight
val snsSaveButtonDarkBackground = snsDarkBg3  // Keep consistent with theme
val snsSaveButtonLightText = snsSaveDefault
val snsSaveButtonDarkText = snsSaveDefault    // Using primary blue for dark theme

// Comment Colors
val snsCommentLightBackground = snsDefaultLight
val snsCommentDarkBackground = snsDarkBg3

// Progress Colors
val snsProgressLightBackground = snsDefaultLight
val snsProgressDarkBackground = snsDarkBg3
val snsProgressLight = snsDefault
val snsProgressDark = snsDarkAccent

// Cursor Colors
val snsCursorLight = snsDefault
val snsCursorDark = snsDarkAccent

// Follow Colors
val snsFollowDefault = Color(0xFFF3F5F7)

// Error
val errorLight = Color(0xFFDC3545)
val errorDark = Color(0xFFE57373)

val snsNaver = Color(0xFF03C75A)
val snsKakao = Color(0xFFFEE500)